# Coverage Loop 重建方案

## 产品流程

1. 选择项目根目录，按需选择 Maven `settings.xml`，确认本地仓库短路径（Windows 默认 `D:/m2`），点击下一步扫描。
2. 展示全部 Reactor 模块及 Java 包/类树；直接勾选模块、包与类，取消即排除，无需手写匹配规则。兼容读取旧 Include / Exclude 配置。设定目标行覆盖率，测试清单从所选生产类所在包自动生成。
3. 选择 Hermes / OpenCode，设置程序路径、Provider、模型、每批类数、最多验证轮数、重复失败熔断和超时；独立编辑补测与失败修复提示词。
4. 保存配置进入看板，运行 Maven 基线或自动补测。看板展示真实轮次，支持停止、回看和打开执行证据。
5. 项目、配置、任务与每轮快照存入 SQLite；原始 Maven / Agent 日志和提示词继续存为文件。

## 技术架构

| 层 | 选择 | 职责 |
|---|---|---|
| 桌面外壳 | Electron | 文件/目录选择、Java 生命周期、受限 IPC、Windows 安装包 |
| 界面 | React + TypeScript + Vite | 三步向导、范围树、提示词编辑、逐轮看板、独立统计、历史 |
| UI | Aceternity UI + Tailwind CSS 4 + Motion + Lucide | Bento 指标卡、Hover Border Gradient 主操作、浅绿色主题、树展开/数字动效、统一标题栏 |
| 执行内核 | Java 17 + JDK HttpServer | 工程扫描、任务状态机、Maven/Agent 子进程、JaCoCo/Surefire 解析 |
| 数据库 | SQLite / sqlite-jdbc | 项目、配置、任务、不可变验证轮次 |
| 构建验证 | Maven Wrapper + JUnit、Vitest、Node test | 真 Maven 样例、持久化、接口认证和界面交互 |

已有 Java 核心可直接抽离，当前使用 JDK 自带的本机 HTTP 服务，避免为桌面进程引入额外服务框架。未来需要 Spring Boot 时可替换 API 层，执行服务与界面接口保持稳定。

## 仓库清理

- 移除 JavaFX 入口、`ui/`、JavaFX CSS、JavaFX Maven 插件与旧 Windows 启动脚本。
- Java 模型、核心服务和回归测试迁入 `backend/`，修复迁移中发现的问题。
- 新建 `apps/desktop/`、统一 npm 命令、SQLite 层、开发与打包脚本、CI。
- 不改写 Git 历史。旧版本可从重建前提交 `d16ee5fed0218a2ae3d7140734cad1542993a279` 找回。

## 数据模型

| 表 | 主键 | 内容 |
|---|---|---|
| projects | root_pom | 工程名称、最近打开时间 |
| configurations | root_pom + id | 完整配置 JSON、提示词、更新时间 |
| statistics_configurations | root_pom + id | 独立统计配置 JSON，同时写入工程配置文件 |
| jobs | id | 工程、状态、开始时间、配置与任务汇总 |
| rounds | job_id + round_number | 本轮测试结果、覆盖率、未达标类、日志路径 |

SQLite 使用 WAL、外键与 busy timeout，所有写入通过同一个同步访问层。`PRAGMA user_version=2` 表示当前版本。首次启动导入已打开工程的旧 `.coverage-loop/configs/*.json` 配置，旧文件保留。重启时将未完成任务标记为 `interrupted`，保留已有轮次；重新运行必须生成新基线，不自动复用上次执行结果。

## 看板统计口径

- **DT 总数**：目标模块本轮 Surefire XML 的执行用例数。参数化测试可能包含多个执行条目，不代表静态测试方法数。
- **DT 变化**：本轮执行数减上一轮执行数，可为负；第一轮为空。不将此数宣称为 Agent 新增方法数量。
- **改动测试文件**：本轮验证前那次 Agent 执行记录的 `changedTestFiles` 数量，新增/修改/删除均可能计入。
- **行覆盖率**：所选范围的 covered lines / (covered + missed)。模块加权汇总，不能直接平均模块百分比。
- **未达标类**：当前覆盖率低于阈值的类，包括基线已达标但后来回退的类。
- **本轮新达标**：上一轮未达标、本轮真实测量达标且仍存在于范围内的类。
- 测试失败和覆盖率未达标分别展示；没有有效 JaCoCo 报告时覆盖率显示“—”，源码估算不作为成功判据。

## 执行规则

- 预构建从根 POM install 所选模块及依赖（跳过测试）；正式测试在根 POM 下对所选模块执行一次 `-pl … -am` reactor，扩展按模块控制测试范围。单模块工程不追加无效的 `-pl .`。
- 测试源文件按所选生产包筛选；整个模块被选中时使用该模块测试源文件。通过 Surefire includesFile / excludesFile 约束 POM 中已有的宽范围 includes；文件内排除补集防止未知或生成的测试越出选择范围。类名不拼接进命令行。
- 扫描针对标准 `src/main/java` / `src/test/java`；自定义源码目录与跨包测试映射需要后续支持。
- 基线与每轮验证前删除目标模块旧 `jacoco.exec` / JaCoCo XML / Surefire TEST XML，防止报告混用。
- Maven 失败进入修复分支，成功后才能检查类级门禁。
- 最大轮数包含第一轮基线。达到上限仍未达标时标记 `limited`，不会显示成功。
- 同一失败连续达到阈值熔断。没有新鲜报告或没有命中目标类停止循环。
- 任务串行，启动后配置锁定。取消标记在子进程启动边界检查；退出时终止 Maven / Agent 子进程树。
- Java 服务仅绑定 `127.0.0.1` 随机端口，随机令牌只在主进程与 Java 服务间传递；拒绝带浏览器 Origin 的请求。
- 默认要求 Agent 只改目标模块 `src/test`，目前这是提示词规则和变更记录，不是操作系统文件隔离；应在独立分支/工作树运行并检查全仓库 diff。

## 已实现与后续边界

本次交付范围是上述完整向导、真实执行、逐轮看板、独立多配置覆盖率统计、SQLite 和 Windows 打包配置。保留旧核心的跨模块业务统计服务，但本次界面聚焦任务看板，没有提供业务分组编辑页面。

后续独立事项：分支覆盖率门禁、生产类到 DT 用例的准确映射、主动暂停/安全断点续跑、多个任务排队、强制文件写入隔离、增量代码差异查看、更多 Agent 适配器、安装包签名与自动更新。当前不把这些列为完成能力。

## 验收

- 真实 Maven 样例能从根目录扫描模块、执行测试并生成有效 JaCoCo。
- Include/Exclude 生效；选择单模块与多模块均可运行。
- 提示词与批次设置保存到 SQLite，重新打开可读取。
- 每轮测试、覆盖率和类清单可恢复；中断任务不会被恢复成成功状态。
- 停止任务不会继续派生下一轮进程。
- React 类型检查与构建通过，关键页面交互测试通过。
- Windows CI 构建安装包；Windows 人工安装、原生目录选择和真实 Agent 验收需在对应环境完成。


## v1.2 · 参赛演示交互完善

已实现：全局底部运行控制台、真实模块生命周期进度、层级包树、未达标类折叠、六文件轮次记录页、指定 ID 的 CLI 会话恢复、浅绿动效。默认每个新任务首轮预 install（跳过测试执行，保留测试编译），后续循环只测范围。无测试模块保持未测并可进入补测；所选测试失败不能成为成功门禁，其他模块继续采集。

验证覆盖：真实 Maven 的成功/失败/无测试混合模块；Reactor 输出跨块与并行解析；包树父子选择和按行汇总；记录轮次隔离及文件白名单；Hermes/OpenCode ID 解析；前端标签切换与迟到响应；Windows PowerShell 实际参数传递。Windows 安装与真实 Agent 已登录环境仍需在目标机器演示验收。


## v1.2.1 · Maven 版本号和历史清理

- 工程首页与统计页独立版本号输入、-U 开关；新旧配置兼容、复制及 SQLite / JSON 持久化；预构建和范围测试统一传参。
- 单条运行记录删除，包含全部轮次；确认后可选择删除该次产物，默认保留文件。运行中记录、越界路径和被其他任务引用的目录受到保护。
- 删除后的当前快照和界面缓存同步清空，历史列表不会因迟到的轮询响应恢复旧记录。

## v1.2.2 · Reactor 依赖兼容修复

- 根 POM + `-pl` + `-am` 保留内部依赖的 reactor 模型；所选模块一次执行，依赖只构建、不跑测试。
- 内置 Java 8 兼容 Maven 生命周期扩展，覆盖继承/执行级测试范围配置；每轮独立清单，用户 POM 保持原样。预构建保留测试编译并强制跳过测试执行。
- 依赖解析失败单独诊断，显示覆盖率无效、待评估；不保存失败基线，不进入 Agent 改码。日志记录真实命令、工作目录。
- 回归覆盖父版本占位符、并行 reactor、中文空格路径、依赖中的同名失败测试、后续轮次无需 install、失败后重新建立基线。
